#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter 2 variable:");
   scanf("%d%d",&a,&b);
    a=a+b;     // a=30 b=20
    b=a-b; // a=30 b=10
    a=a-b; // a=20  b=10
    printf("\na=%d \nb=%d",a,b);
    return 0;
}
