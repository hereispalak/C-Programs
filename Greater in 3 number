#include<stdio.h>

int main() {
    int a,b,c;
    printf("enter 3 number:");
    scanf("%d%d%d",&a,&b,&c);
    if(a>b && a>c)
    printf("\nA is greater");
    if(b>c && b>a)
    printf("\n b is greater");
    if(c>a && c>b)
    printf(" \nc is greater ");
    return 0;
    
}
